org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Kotlin incremental compilation: Kotlin 2.x's Build Tools API cache
# fails to close on Windows ("Could not close incremental caches"), breaking
# release builds. Full (non-incremental) compilation is reliable here.
kotlin.incremental=false
org.gradle.java.home=C:/Program Files/Android/Android Studio/jbr